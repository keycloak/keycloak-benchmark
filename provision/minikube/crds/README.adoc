To download all CRDs of the current Kubernetes instance, run the follwing script:

[source,bash]
----
./dumpcrds.sh
----

Afterwards, the IDE can use the CRDs for autocompletion and validation when writing Helm charts.
